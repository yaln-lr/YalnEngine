# Vulkan 四面体渲染程序 - 键盘控制说明

## 概述
这个程序使用Vulkan渲染一个旋转的四面体，支持用户通过键盘控制旋转、平移和缩放。

## 键盘控制

### 旋转控制
- `左箭头` / `右箭头`: 向左/向右旋转
- `上箭头` / `下箭头`: 向上/向下旋转

### 平移控制
- `W` / `S`: 沿Z轴向前/向后移动
- `A` / `D`: 沿X轴向左/向右移动
- `Q` / `E`: 沿Y轴向上/向下移动

### 缩放控制
- `+` / `=`: 放大
- `-` / `_`: 缩小

### 重置
- `R`: 重置所有变换到初始状态

## 初始状态
- 旋转角度: 0度
- 位置: (0, 0, -3)
- 缩放: 1.0

## 技术特性
- 使用GLM库进行矩阵运算
- 支持统一缓冲区(UBO)实现动态变换
- 使用索引缓冲区进行高效渲染
- 实现透视投影和视图矩阵
- 支持颜色顶点属性

## 编译说明
1. 确保已安装Vulkan SDK和GLM库
2. 使用CMake构建项目
3. 着色器会在构建时自动编译

## 着色器文件
- `shader.vert`: 顶点着色器，处理顶点变换
- `shader.frag`: 片段着色器，处理颜色输出
